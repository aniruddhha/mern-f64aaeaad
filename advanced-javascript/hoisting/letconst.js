console.log(proto)
let proto;

// in the case of var, js hoists(moves) to the top of scope 
// with undefined initilization to proto

// in the case of let and const, js hoists(moves) to the top of scope 
// without any initilization